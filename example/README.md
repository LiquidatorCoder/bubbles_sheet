# bubbles_sheet example

Run it from this directory:

```sh
flutter run
```

Each button opens a sheet exercising one part of the API — the default
`fit`/`large` detents, a three-detent scrollable sheet, a trailing header
action driving the sticky CTA, and the dark palette.
